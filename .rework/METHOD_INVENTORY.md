# Method Inventory — datp-cp

Generated by: (pending — agents must populate this)

This inventory covers all public and internal methods/functions in src/datp/.

For each method, record: module path, signature, inputs, outputs, side effects, and scientific role.

---

## Status

Not yet populated.

Agents must create a CLAUDE task to generate this inventory if it is empty when needed.

---

## Template

For each method:

    ### <module.ClassName.method_name> or <module.function_name>
    Module: src/datp/...
    Signature: <full signature>
    Inputs: <list of parameter name: type>
    Outputs: <return type and description>
    Side effects: <file writes, network calls, mutable state changes, or "none">
    Scientific role: <how this contributes to calibration poisoning, threshold computation, etc., or "none">
    Roadmap reference: <section of DATP_CP_Roadmap.md or "none">
    Obsolete: <yes/no — if yes, name the ticket that removes it>
