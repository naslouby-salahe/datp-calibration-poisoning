# Input/Output Inventory — datp-cp

Generated by: (pending — agents must populate this)

This inventory covers all significant function inputs and outputs, all file I/O paths, and all artifact paths in the datp-cp codebase.

---

## Status

Not yet populated.

---

## 1. File I/O Paths

Expected artifact output root:

    outputs/conference_calibration_poisoning/

Expected per-cell layout:

    outputs/<stage>/<policy>/<seed>/
        manifest.json
        metrics.json
        DONE.txt

Data input root (symlink):

    data/raw -> /home/naslouby/Projects/datp-shared-data/raw

Processed data:

    data/processed/nbaiot/

---

## 2. Artifact Schema

Each output cell must contain:

    manifest.json   — run parameters, timestamps, version
    metrics.json    — result metrics (CV(FPR), AUROC, coverage_ratio, etc.)
    DONE.txt        — written last, signals completion

Forbidden files indicating incomplete run:

    metrics.json.tmp
    IN_PROGRESS

---

## 3. Function I/O Register

(Agents populate this during inventory.)

Template:

    ### <function_name>
    Module: src/datp/...
    Inputs:
      - <param>: <type> — <description>
    Outputs:
      - <type> — <description>
    File reads: <paths or "none">
    File writes: <paths or "none">
    Scientific invariants: <any invariant this function must preserve>
