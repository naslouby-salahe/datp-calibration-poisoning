# CP2-T039 — Smoke Invariants 7–11

**Phase:** D — Smoke validation
**Type:** implementation
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T038
**Blocks:** CP2-T042
**Expected test level:** integration (synthetic)
**Graphify required:** conditional
**Paper notes required:** no

## 1. Purpose
Extend the smoke harness with the remaining five protocol invariants covering B4
decomposition, statistics structure, manifest completeness, AUROC invariance, and
coverage reporting.

## 2. Pre-start audit
Re-read CP2-T038 harness; roadmap §11 (invariants 7–11).

## 3. Scope
Assert:
7. B4 `Δτ_total = Δτ_agg + Δτ_churn` (within tolerance); client-indexed.
8. Statistics are two-layer (paired per-victim deltas → 5 seed aggregates; not 45
   independent); bootstrap CI computed on 5 aggregates.
9. Manifest round-trips with all child seeds, locks, reservoir mode,
   `mu_flag_threshold`.
10. AUROC ≈ unchanged clean vs poisoned (calibration-only attack).
11. CV(FPR) reported **with coverage**; CV(FPR) uses no ε.

## 4. Non-goals
- No real data; no new core features.

## 5. Scientific locks
All CP2 locks; deterministic; CPU-only.

## 6. Coding locks
Typed; meaningful assertions; reuse harness; no CUDA.

## 7. Implementation guidance
Extend the CP2-T038 harness; keep runtime small.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_smoke
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Invariants 7–11 pass on synthetic data; full 11-invariant suite green; pyright green.

## 12. Blocking conditions
Any invariant failure → fix the owning core ticket.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Harness diff, full smoke output, progress entry.
