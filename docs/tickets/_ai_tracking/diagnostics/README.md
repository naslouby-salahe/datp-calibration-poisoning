# CP2 Diagnostics

Store diagnostic outputs (small dry-runs, one-seed/one-victim probes, smoke
artifacts, sanity checks) here. Name files `CP2-TXXX_<short>.md` or `.json`.

Diagnostics are evidence, not results. They must never be cited as final
experimental claims. Do not place full MVP/Full experiment outputs here.
