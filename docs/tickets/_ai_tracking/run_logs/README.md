# CP2 Run Logs

Store short run-log notes and pointers to console logs here. Real console logs are
captured by the Makefile under `outputs/console_logs/`; this folder holds curated
summaries and links, not bulk logs.

Each entry: date, ticket id, command, scale (SMOKE/MVP/FULL/STRETCH), outcome,
artifact path, anomalies.
