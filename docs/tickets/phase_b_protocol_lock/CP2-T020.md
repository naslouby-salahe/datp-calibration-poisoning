# CP2-T020 — [REFACTOR] Enums/Config/Constants Consolidation Checkpoint

**Phase:** B — Protocol lock
**Type:** refactor
**Priority:** medium
**Status:** not started
**Dependencies:** CP2-T016 … CP2-T019
**Blocks:** —
**Expected test level:** unit, static, graphify
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Periodic refactor checkpoint: ensure the new enums, config schema, constants/paths,
and manifest/seed code form one coherent, non-duplicated foundation.

## 2. Pre-start audit
Re-read the modules added in CP2-T016–T019 and confirm single ownership per concept.

## 3. Scope
- Centralize any duplicated constants/enums; remove unsafe defaults and untyped
  structures; ensure typed dataclasses/config models everywhere.
- Confirm no `shift_magnitude`-style hardcoded science remains.
- Run pyright across the new modules and Graphify for structure.

## 4. Non-goals
- No new features; consolidation only.

## 5. Scientific locks
B3 absent from default policy; all locked scalars come from config/constants; E=1
enforced; SeedSequence scheme intact.

## 6. Coding locks
No backward compatibility; centralized enums/constants; typed config; no duplication;
no stale legacy code.

## 7. Implementation guidance
Move/merge where ownership is unclear; delete retired prototype modules with no
remaining references.

## 8. Tests and diagnostics
```text
pytest tests/unit/{core,config,artifacts,attacks} -q
pyright
make lint
```

## 9. Graphify
Run `graphify .` if available; save under `_ai_tracking/graphify/`. Else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- One canonical home per enum/constant/config concept; tests + pyright + lint green;
  Graphify run/deferred.

## 12. Blocking conditions
If consolidation reveals a protocol gap, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Refactor diff summary, test/pyright/lint output, Graphify status, progress entry.
