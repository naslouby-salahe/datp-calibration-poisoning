# CP2-T022 — Scientific Guardrails & CLI Stage Layout

**Phase:** B — Protocol lock
**Type:** implementation
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — `src/datp/config/stages.py` + `attacks/guardrails.py` + `app/cli/poison.py` + tests; all stages allow_run=False.
**Dependencies:** CP2-T017
**Blocks:** CP2-T028
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Define runtime scientific guardrails (assertions/validators) and the CP2 CLI/config
stage layout so runs are gated and reproducible.

## 2. Pre-start audit
Inspect:
```text
src/datp/app/cli/*           # existing Typer subcommands
src/datp/conf/*  config.yaml regime/*
docs/DATP_CP_Roadmap.md §10 (orchestration, stages)
```

## 3. Scope
- Stage-scoped configs: `common`, `audit_readonly`, `nbaiot_smoke`, `nbaiot_mvp`,
  `nbaiot_full`, `ciciot2023_stretch`, `paper_figures`.
- Guardrails: no in-place mutation of clean arrays; test/training scores never a
  reservoir; B3 not selectable as default policy; SINGLE_CLIENT for MVP; fractions
  restricted to the locked grid per scale.
- CLI surface for CP2 stages (preview/dry-run/smoke), consistent with inherited CLI.

## 4. Non-goals
- No experiment execution.
- No heavy/Full stages wired to run by default.

## 5. Scientific locks
All CP2 locks (README §9); guardrails enforce them at boundaries.

## 6. Coding locks
Typed config/CLI; explicit validation errors; no unsafe defaults; no duplication.

## 7. Implementation guidance
Add CP2 subcommand(s) mirroring `datp sweep`/`config preview` style; keep heavy
stages behind explicit flags.

## 8. Tests and diagnostics
```text
pytest tests/unit/app -q
pytest tests/unit/config -q
pyright
```

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Stage configs + guardrails + CLI present; smoke/dry-run reachable; heavy stages
  gated; tests + pyright green.

## 12. Blocking conditions
If CLI patterns clash with inherited Typer app, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
CLI/config diff, tests + pyright, progress entry.
