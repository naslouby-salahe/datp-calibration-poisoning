# CP2-T018 — CP2 Constants & Path Registry

**Phase:** B — Protocol lock
**Type:** implementation
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T016
**Blocks:** CP2-T044
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Centralize CP2 constants and the canonical output path builder so no module
hardcodes artifact names, run paths, or scientific scalars.

## 2. Pre-start audit
Inspect:
```text
src/datp/artifacts/{constants,layout,names,paths}.py
docs/DATP_CP_Roadmap.md §10 (run-path), §7 (manifests)
```

## 3. Scope
- Canonical run-path builder:
  `outputs/conference_calibration_poisoning/<scale>/<dataset>/<policy>/<objective>/<source>/f_<fraction>/scope_<scope>/train_<seed>/poison_<seed>/`.
- Centralized constants: seed lists, `n_min=100`, tail mass 0.10, materiality factor
  0.1, B4 hyperparameters, manifest filenames
  (`project_audit_report.json`, `clean_score_artifacts.json`,
  `nbaiot_mvp_manifest.json`, `paper_figure_manifest.json`).
- Reuse inherited artifact path patterns where they fit; do not duplicate.

## 4. Non-goals
- No baseline-specific score directories for B1/B2/B4 (scores are shared).
- No scattered path string construction.

## 5. Scientific locks
Outputs only under `outputs/conference_calibration_poisoning/`; clean score
artifacts shared across policies; manifests record E=1, reservoir mode,
`mu_flag_threshold`, child seeds.

## 6. Coding locks
Centralized constants/paths; typed path builders; no hardcoded artifact names; no
duplication.

## 7. Implementation guidance
Add a `cp2` namespace under `artifacts/` or extend existing modules; one owner per
concept.

## 8. Tests and diagnostics
```text
pytest tests/unit/artifacts -q
pyright
```

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Single path builder + constants module; tests assert path shape; pyright green.

## 12. Blocking conditions
If path layout conflicts with inherited DATP layout, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Constants/path module diff, tests + pyright, progress entry.
