# CP2-T016 — CP2 Enum Definitions

**Phase:** B — Protocol lock
**Type:** implementation
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T010, CP2-T015
**Blocks:** CP2-T017, CP2-T027
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Define the stable CP2 enums centrally so all downstream code references typed
domain values, not strings.

## 2. Pre-start audit
Inspect before adding:
```text
src/datp/core/enums.py            # existing Baseline/Regime/ThresholdSource
src/datp/attacks/calibration_poisoning.py  # legacy PoisoningObjective
docs/DATP_CP_Roadmap.md §10 (enums)
```
Decide whether CP2 enums live in `core/enums.py` or a dedicated `attacks/enums.py`
that re-exports — pick one canonical home; no duplicate definitions.

## 3. Scope
Define (StrEnum, canonical home):
```text
ThresholdPolicy            = {B1_GLOBAL, B2_PERSONALIZED, B4_CLUSTER}   # B3 EXCLUDED
AttackerObjective          = {THRESHOLD_RAISE, THRESHOLD_LOWER}
PoisoningSourceStrategy    = {RANDOM_BENIGN, HIGH_SCORE_BENIGN, LOW_SCORE_BENIGN,
                              LOW_SCORE_TARGETED_REMOVAL_DIAGNOSTIC_ONLY}
CalibrationInjectionRule   = {REPLACE_FIXED_BUDGET}   # diagnostic-only appended later
PoisoningKnowledge         = {GRAY_BOX_SCORE_ACCESS, WHITE_BOX_DIAGNOSTIC_ONLY}
PoisoningTargetScope       = {SINGLE_CLIENT, MULTI_CLIENT, ALL_CLIENTS_DIAGNOSTIC_ONLY}
PoisoningDefense           = {NONE, TRIMMED_CALIBRATION}
ExperimentScale            = {SMOKE, MVP, FULL, STRETCH}
AuditDisposition           = {KEEP_CORE, REFACTOR_CORE, QUARANTINE_JOURNAL,
                              REMOVE_STALE, BLOCK_UNSAFE}
```
Replace/retire the legacy `PoisoningObjective` (map to `AttackerObjective`).

## 4. Non-goals
- **B3 must not appear** in `ThresholdPolicy` (appendix-only behind an explicit CLI
  flag, separate from the default enum).
- No behavior/logic here — definitions only.

## 5. Scientific locks
Default policies exclude B3; objectives/sources/scopes/defenses as listed;
diagnostic-only members clearly named.

## 6. Coding locks
No backward compatibility (retire legacy enum, do not alias); centralized enums; no
duplicated definitions; no `Any`; no AI comments; no TODO; typed.

## 7. Implementation guidance
Add enums to the chosen canonical module; update the few existing prototype imports
to the new names (or remove if the prototype is being replaced).

## 8. Tests and diagnostics
```text
pytest tests/unit/core/test_enums.py -q
pytest tests/unit/attacks -q
pyright
```

## 9. Graphify
Run `graphify .` if available after the move; save under `_ai_tracking/graphify/`.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- All CP2 enums defined once, centrally; B3 excluded from `ThresholdPolicy`; legacy
  enum retired; unit tests + pyright green.

## 12. Blocking conditions
If enum placement conflicts with shared DATP enums, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Enum module diff, test + pyright output, Graphify status, progress entry.
