# CP2-T023 — [DRIFT] Protocol-Lock Scientific Drift Check

**Phase:** B — Protocol lock
**Type:** scientific-drift
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T016, CP2-T020
**Blocks:** CP2-T024
**Expected test level:** unit, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Scientific-drift gate for the protocol-lock phase: confirm the enums, config,
constants, manifest, validation, and CLI layout encode the CP2 protocol without
drift before core implementation.

## 2. Pre-start audit
Compare the locked architecture against `docs/DATP_CP_Roadmap.md` (§5, §6, §7, §10,
§12) and `Additional_Docs/Synthesis/Claims.md`.

## 3. Scope
Verify and fix (where safe):
- `ThresholdPolicy` excludes B3; objectives/sources/scopes/defenses/scale correct.
- E=1 enforced; SeedSequence scheme (no integer addition).
- victim-local reservoirs; test/training scores never reservoirs; no in-place
  mutation guardrail present.
- no journal contamination; no forbidden datasets/comparators; no broad claims.

## 4. Non-goals
- No new features.

## 5. Scientific locks
All CP2 locks (README §9).

## 6. Coding locks
Verify typed config/enums/constants; no hardcoded science; no duplication.

## 7. Implementation guidance
Write a drift report under `_ai_tracking/audits/`; fix code/doc drift or write a
blocking decision.

## 8. Tests and diagnostics
```text
pytest tests/unit/{core,config,artifacts,validation,app} -q
pyright
rg -ni "B3|shift_magnitude|integer seed|edge-?iiot|fedprox|ditto|laridi" src
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Confirm claim-discipline alignment; refresh do-not-claim reminders.

## 11. Acceptance criteria
- Drift report written; locked architecture confirmed faithful or corrected/blocked.

## 12. Blocking conditions
If a protocol conflict is found that cannot be safely fixed, hard-stop with a
decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Drift report, test/pyright output, paper-notes entry, progress entry.
