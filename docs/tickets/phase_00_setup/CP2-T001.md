# CP2-T001 — Initial Repository Audit

**Phase:** 00 — Setup
**Type:** audit
**Priority:** critical
**Status:** done (2026-06-15)
**Dependencies:** CP2-T000
**Blocks:** CP2-T007 (and all implementation phases)
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** no

## 1. Purpose
Produce/refresh the authoritative read-only audit of the repository so the ticket
plan is grounded in reality. A baseline audit already exists at
`_ai_tracking/audits/CP2_INITIAL_REPO_AUDIT.md`; verify it against the current tree
and update anything stale.

## 2. Pre-start audit
Re-inspect (do not trust the existing audit blindly):
```text
src/datp/{attacks,experiments,thresholding,scoring,statistics,validation,reporting,core}
tests/{unit,integration,e2e,fixtures}
Makefile  COMMANDS.md  pyproject.toml  pyrightconfig.json  README.md
.claude/  .github/  docs/DATP_CP_Roadmap.md
```

## 3. Scope
- Confirm the existing audit's findings still hold (CP2 prototype mismatch,
  reusable DATP substrate, no journal contamination in code, Graphify absent).
- Record exact current file inventory of `src/datp/attacks/` and any
  `src/datp/experiments/calibration_poisoning.py` / `diagnostic.py`.
- Update `CP2_INITIAL_REPO_AUDIT.md` if reality has changed.

## 4. Non-goals
- No code changes. Read-only.
- No ticket re-planning unless a finding invalidates the index (then record it).

## 5. Scientific locks
Calibration-channel only; `REGIME_A_NBAIOT` primary; default policies
`{B1_GLOBAL,B2_PERSONALIZED,B4_CLUSTER}`, **B3 excluded**; objectives
`{THRESHOLD_RAISE,THRESHOLD_LOWER}`; sources `{RANDOM,HIGH,LOW}_*BENIGN`;
fractions `{0,0.10,0.20,0.40}`; `REPLACE_FIXED_BUDGET`; E=1 only.

## 6. Coding locks
Read-only audit. Note any code violating: enum centralization, no hardcoded
science, typed config, no in-place mutation, no journal logic.

## 7. Implementation guidance
Use `list_dir`, `grep_search`, targeted `read_file`. Write findings to
`_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
make help            # enumerate available targets
rg -n "graphify|Edge-IIoT|FedProx|Ditto|Laridi|E=5|shift_magnitude" src docs
```

## 9. Graphify
Run `graphify update .` (no API key needed); else record deferral in the Graphify status file.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- `CP2_INITIAL_REPO_AUDIT.md` reflects current reality.
- Any divergence from the index is recorded.

## 12. Blocking conditions
If repository structure is drastically different from the audit or the protocol,
stop and write a decision record (hard-stop rule).

## 13. Tracking update
Append to `_ai_tracking/progress/CP2_PROGRESS.md`.

## 14. Completion evidence
Updated audit file, grep outputs captured in the audit note, progress entry.
