# CP2-T005 — [REFACTOR] Setup, Tracking, and Agent-Config Consistency Checkpoint

**Phase:** 00 — Setup
**Type:** refactor
**Priority:** medium
**Status:** not started
**Dependencies:** CP2-T000, CP2-T001, CP2-T002, CP2-T003, CP2-T004
**Blocks:** —
**Expected test level:** static, graphify
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Periodic refactor checkpoint for the setup phase: ensure tickets, tracking files,
README, index, Graphify status, and agent config are mutually consistent and free
of duplication or stale references before audit work begins.

## 2. Pre-start audit
Re-read README, TICKET_INDEX, the tracking files, and the `.claude`/`.github`
edits from CP2-T003. Confirm IDs, links, and dependencies are coherent.

## 3. Scope
- Centralize any duplicated lock text by pointing to `docs/tickets/README.md` §9.
- Remove stale/contradictory references introduced during setup.
- Verify the index counts and dependency edges are accurate.
- Run Graphify (or record deferral) to sanity-check repo structure.

## 4. Non-goals
- No production code changes.
- No new tickets unless a real gap is found (record it first).

## 5. Scientific locks
No scientific behavior here; ensure lock text remains accurate and consistent.

## 6. Coding locks
No backward compatibility; centralize constants/lock text; no duplicated docs; no
stale references.

## 7. Implementation guidance
Docs-only edits under `docs/tickets/**` and (if needed) `.claude`/`.github`.

## 8. Tests and diagnostics
```text
make lint
make typecheck
```
(Run only if config edits could affect tooling.)

## 9. Graphify
Run `graphify update .` (no API key needed); save output under `_ai_tracking/graphify/`. Else
record deferral with reason.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Setup docs/config internally consistent; index accurate; Graphify run/deferred.

## 12. Blocking conditions
If inconsistencies cannot be resolved without scope changes, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Consistency note, Graphify output/deferral, progress entry.
