# CP2-T011 — Thresholding & B4 Procedure Audit

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T001
**Blocks:** CP2-T015, CP2-T031, CP2-FB3
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Audit the inherited thresholding code (B1/B2/B4) for CP2 reuse and verify the B4
procedure matches the locked specification. This is the precursor to the B4
reproducibility gate (CP2-FB3).

## 2. Pre-start audit
Read-only:
```text
src/datp/thresholding/thresholds.py  eligibility.py
src/datp/thresholding/strategies/{b1_global,b2_personalized,b4_cluster}.py
src/datp/core/enums.py  (Baseline, ThresholdSource, B4_FINGERPRINT_FEATURES)
docs/DATP_CP_Roadmap.md §5 (B4 spec, decomposition), §13 FB3
```

## 3. Scope
- Confirm B1 = arithmetic mean of eligible client thresholds; B2 = per-client
  percentile; Calibration-Pending → `tau_global`.
- Verify B4: fingerprint `[mean,std,skew,p95]` (locked order), k-means++,
  `n_init=10`, `max_iter=300`, `random_state=42`, and **K=3 on N-BaIoT** (not
  silently silhouette-overridden). Record current config values vs locked spec.
- Confirm client-indexed effective thresholds are derivable (never raw cluster
  labels) to support the decomposition `Δτ_total = Δτ_agg + Δτ_churn`.

## 4. Non-goals
- No code changes; read-only.
- Do not run full B4 reproduction here (that is CP2-FB3 if needed).

## 5. Scientific locks
B4 N-BaIoT K=3; fingerprint order locked; deltas client-indexed; decomposition
sums exactly; Calibration-Pending excluded from clustering.

## 6. Coding locks
Read-only; flag any hardcoded/divergent B4 hyperparameters for Phase C.

## 7. Implementation guidance
Write a B4-spec conformance note under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
pytest tests/unit/thresholding/strategies/test_b4_cluster.py -q
pytest tests/unit/thresholding -q
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Note that B4 reuses the "DATP clustering procedure unchanged" (reviewer-risk §14).

## 11. Acceptance criteria
- B4 conformance documented; divergences listed; FB3 trigger decided (yes/no).

## 12. Blocking conditions
If B4 cannot be made to match the locked spec from inherited code, flag CP2-FB3.

## 13. Tracking update
Update Phase-A confirmation #3 readiness in progress.

## 14. Completion evidence
B4 conformance note, pytest output, progress + confirmation update.
