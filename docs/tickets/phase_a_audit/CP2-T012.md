# CP2-T012 — Scoring/Statistics/Reporting Reuse Audit

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — `_ai_tracking/audits/CP2-T012_statistics_reporting_audit.md`.
**Dependencies:** CP2-T001
**Blocks:** CP2-T033, CP2-T035
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Catalog reusable scoring, statistics, and reporting machinery for CP2 metrics and
the seed-level statistical plan, and identify gaps (sign test, seed-level
aggregates, strict-majority rule, CP2 figures/tables).

## 2. Pre-start audit
Read-only:
```text
src/datp/scoring/{loading,cal_loading,schema}.py
src/datp/statistics/{bootstrap,cv,wilcoxon,effect_size,spearman,constants}.py
src/datp/reporting/{build,figures,tables,engine}.py
docs/DATP_CP_Roadmap.md §8 (metrics), §9 (statistics), §11 (figures/tables)
```

## 3. Scope
- Confirm `bootstrap_ci` (percentile) + `bca_ci`, `cv` (σ/µ), `wilcoxon_test`
  exist; check whether an **exact paired sign test** and **seed-level aggregate**
  helpers exist (likely missing → Phase C CP2-T035).
- Map CP2 metrics to existing code: `CV(FPR)`, IQR(FPR), max−min FPR,
  WorstClientFPR, AUROC; identify `Δτ`/`Δτ_rel`/`δ_τ,i` as new.
- Identify CP2 figure/table needs (F1–F6, T1–T6) vs inherited reporting.

## 4. Non-goals
- No code changes; read-only.

## 5. Scientific locks
`CV(FPR)=σ/µ` no ε; two-layer unit (never 45 independent); ≥4/5 sign consistency;
bootstrap on 5 seed-level aggregates; Holm/Wilcoxon descriptive/supporting only.

## 6. Coding locks
Read-only; flag duplication or hardcoded stats constants for Phase C.

## 7. Implementation guidance
Write a reuse/gap map under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
pytest tests/unit/statistics -q
pytest tests/unit/reporting -q
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Record which figures/tables are reusable vs new for CP2 (F3 Δτ, F4 ΔCV(FPR),
F5 vulnerability profile, F6 failure-mode separation).

## 11. Acceptance criteria
- Reuse/gap map complete with concrete Phase-C implications.

## 12. Blocking conditions
None expected.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Reuse/gap map note, pytest output, paper-notes entry, progress entry.
