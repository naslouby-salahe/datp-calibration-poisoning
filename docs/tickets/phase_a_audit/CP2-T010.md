# CP2-T010 — Existing CP2 Attack-Code Audit (Prototype Assessment)

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T001
**Blocks:** CP2-T016, CP2-T027
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** no

## 1. Purpose
Assess the existing CP2 attack prototype against the protocol so Phase B/C can
decide exactly what to replace. The audit found the prototype uses an
`attack_rate` + `shift_magnitude` add/subtract model that does NOT match
`REPLACE_FIXED_BUDGET`.

## 2. Pre-start audit
Read-only:
```text
src/datp/attacks/calibration_poisoning.py
src/datp/attacks/poisoning_config.py
src/datp/attacks/poisoning_metrics.py
src/datp/experiments/calibration_poisoning.py
tests/unit/attacks/*  tests/integration/attacks/*
docs/DATP_CP_Roadmap.md §6, §10
```

## 3. Scope
- Document exact gaps vs protocol: enum names (`PoisoningObjective` vs
  `AttackerObjective`), missing source/injection/knowledge/scope/defense/scale
  enums, `shift_magnitude` (non-protocol), no victim-local tail resampling, no
  `SeedSequence` child seeds, no manifest/E=1 gate.
- Decide per file: keep / refactor / replace / quarantine (with reasons).
- List which existing tests must be rewritten vs reused.

## 4. Non-goals
- No code changes here (read-only audit). Replacement happens in Phase B/C.

## 5. Scientific locks
`REPLACE_FIXED_BUDGET` with victim-local with-replacement resampling; no in-place
mutation; objectives `{THRESHOLD_RAISE,THRESHOLD_LOWER}`; sources
`{RANDOM,HIGH,LOW}_*BENIGN`; SeedSequence child seeds (no integer addition).

## 6. Coding locks
Read-only; record where prototype violates: enums, hardcoded science
(`shift_magnitude`), in-place mutation, typed config, seed scheme.

## 7. Implementation guidance
Write a disposition table (file → disposition → reason) under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
pytest tests/unit/attacks -q        # observe current prototype behavior
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Disposition table written; replacement plan clear for Phase B/C tickets.

## 12. Blocking conditions
None expected; if the prototype is entangled with shared DATP code, record the
coupling for the refactor tickets.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Disposition audit note, pytest output snapshot, progress entry.
