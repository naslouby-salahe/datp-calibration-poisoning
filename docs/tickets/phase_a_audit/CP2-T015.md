# CP2-T015 — Phase-A Gate Report

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T007, CP2-T008, CP2-T011, CP2-T014
**Blocks:** CP2-T016 (protocol lock)
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Produce the Phase-A gate report that authorizes the Phase-B protocol lock. It
records the required protocol confirmations, the non-gating out-of-scope
venue/deadline/submission status, and the pre-lock plans for the bootstrap
variant and `mu_flag_threshold`.

## 2. Pre-start audit
Aggregate evidence from CP2-T007/T008/T011/T012/T014. Re-verify any stale finding.

## 3. Scope
Confirm and record:
1. **E=1** clean score artifacts (else FB1).
2. **DATP bootstrap variant** — locate in the conference training/evaluation or
   threshold module; inherit verbatim; else default **percentile**.
3. **B4 procedural reproducibility** vs DATP per-seed assignments/metrics (else FB3).
4. Venue/deadline/submission strategy is intentionally out of scope for this
   CP2 cleanup and must not gate protocol or ticket progress.
Also record the pre-lock plan to compute `M_clean` (clean B1 eligible-client mean
FPR) and lock `mu_flag_threshold = round(M_clean/8, 2 s.f.)` **before any poisoned
run** (computed later, in MVP prep, but planned here).

## 4. Non-goals
- No code changes; no poisoned runs.

## 5. Scientific locks
E=1; bootstrap on 5 seed-level aggregates (variant per confirmation #2);
`mu_flag_threshold` is a CV-instability flag, not a denominator stabilizer.

## 6. Coding locks
Read-only.

## 7. Implementation guidance
Write `CP2_PHASE_A_GATE_REPORT.md` under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
rg -n "bootstrap|percentile|bca" src/datp/statistics src/datp/reporting
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Record the chosen bootstrap variant and baseline reproducibility wording.

## 11. Acceptance criteria
- Protocol confirmations recorded (pass or fallback-triggered); non-gating
  venue/deadline/submission status recorded; bootstrap variant decided;
  `mu_flag_threshold` lock plan documented.

## 12. Blocking conditions
If any confirmation fails, trigger the matching fallback (FB1/FB3) or record the
non-gating out-of-scope venue/deadline/submission status in the gate report.

## 13. Tracking update
Update the Phase-A confirmation/status rows in `CP2_PROGRESS.md`.

## 14. Completion evidence
Gate report, progress confirmations updated, paper-notes entry.
