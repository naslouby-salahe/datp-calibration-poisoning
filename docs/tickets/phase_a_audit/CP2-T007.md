# CP2-T007 — Clean Score Artifact Discovery, Provenance & E=1 Audit

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T001, CP2-T006
**Blocks:** CP2-T015, CP2-FB1
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Determine whether CP2-controlled clean N-BaIoT per-client score artifacts exist in
this repository and are confirmed under **E=1**. This is Phase-A confirmation #1
and the FB1 gate.

## 2. Pre-start audit
Read-only inspect:
```text
src/datp/scoring/{generation,loading,cal_loading,schema}.py
src/datp/validation/{score_manifest,results,invariants}.py
outputs/** (scores/checkpoints/manifests if present)
docs/DATP_CP_Roadmap.md §7, §13 (FB1)
```
Do not trust progress files alone.

## 3. Scope
- Locate persisted score artifacts (`reconstruction_error` column; cal/test_benign/
  test_attack splits) for Regime A and confirm per-client calibration scores exist.
- Verify provenance manifest: CP2-generated within this repository, conference protocol, **E=1**,
  checkpoint/round, DATP split semantics, no journal-only variants, safe read-only
  paths. **Any E=5 artifact is rejected.**
- Record whether artifacts exist + pass, or are missing/insufficient (→ FB1).

## 4. Non-goals
- No retraining (that is FB1 / CP2-FB1 if triggered).
- No code changes; read-only.

## 5. Scientific locks
E=1 only; reject E=5; clean artifacts must be CP2-generated in this repository,
following conference-faithful DATP protocol settings; scores shared across B1/B2/B4
(no baseline subdir); test/training scores are not reservoirs.

## 6. Coding locks
Read-only audit; note any provenance code gaps for Phase B.

## 7. Implementation guidance
Write `clean_score_artifacts.json` evidence (or a notes file) under
`_ai_tracking/manifests/` and an audit note under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
rg -n "E=5|local_epochs|epochs" src docs outputs 2>/dev/null
ls -R outputs 2>/dev/null | head -80
```

## 9. Graphify
Optional; record deferral if unavailable.

## 10. Paper-writing notes
Note whether clean baselines are reproducible "under the published DATP conference
protocol" (safe wording) — gates manuscript baseline language.

## 11. Acceptance criteria
- Clear verdict: artifacts present + E=1 confirmed, OR trigger CP2-FB1 with evidence.

## 12. Blocking conditions
If provenance cannot be confirmed and no artifacts exist, activate CP2-FB1 via a
decision record (do not retrain here).

## 13. Tracking update
Update Phase-A confirmation #1 in `CP2_PROGRESS.md`.

## 14. Completion evidence
Provenance manifest/notes, audit note, progress + confirmation update.
