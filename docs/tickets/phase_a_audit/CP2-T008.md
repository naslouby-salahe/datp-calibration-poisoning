# CP2-T008 — DATP Split Semantics Audit

**Phase:** A — Read-only audit
**Type:** audit
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T007
**Blocks:** CP2-T015
**Expected test level:** static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Confirm the DATP chronological split semantics (train / calibration / test) and
identify whether artifacts expose victim-local benign **calibration-candidate**
scores not used to fit the clean threshold (source-precedence rule, roadmap §6).

## 2. Pre-start audit
Read-only:
```text
src/datp/data/** (splits, contracts, manifests)
src/datp/scoring/cal_loading.py
docs/DATP_CP_Roadmap.md §7 (calibration/eligibility), §6 (sources)
```

## 3. Scope
- Document the split (e.g., 60% train / gap / 20% calibration / gap / ~19% test)
  per device and confirm benign-only calibration.
- Determine the reservoir source per precedence: (1) victim-local benign
  calibration-candidate scores if exposed; else (2) resample with replacement from
  the victim's clean calibration distribution (tail for HIGH/LOW; full pool for
  RANDOM). Record which path applies and note the abstraction for the manifest.
- Confirm `n_min = 100` eligibility and Calibration-Pending handling.

## 4. Non-goals
- No new splits unless absolutely required (would need `split_seed=[200..204]` and a
  decision record).
- No code changes; read-only.

## 5. Scientific locks
Benign-only calibration; `n_min=100`; Calibration-Pending receive `tau_global`,
excluded from CV(FPR) and the victim set; replacement preserves eligibility; test
scores never a reservoir; training scores not a reservoir.

## 6. Coding locks
Read-only; note gaps for Phase C reservoir design (CP2-T026).

## 7. Implementation guidance
Write the split/source-precedence findings to `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
rg -n "calibration|n_min|chronological|split" src/datp/data src/datp/thresholding
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Record the disclosed score-level proxy abstraction (and high-fraction with-
replacement repetition) as a limitation to surface in the manuscript.

## 11. Acceptance criteria
- Split semantics documented; reservoir source path decided with evidence.

## 12. Blocking conditions
If calibration-candidate semantics are ambiguous and the fallback resampling cannot
be justified, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Split/source audit note, paper-notes limitation entry, progress entry.
