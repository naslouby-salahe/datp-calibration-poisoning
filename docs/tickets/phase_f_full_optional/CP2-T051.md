# CP2-T051 — Multi-Client Pairs & 20 Triples (Independent Co-Victim Streams)

**Phase:** F — Full / optional (CONDITIONAL on CP2-T049 = CONTINUE)
**Type:** implementation
**Priority:** medium (conditional)
**Status:** not started (gated by CP2-T049)
**Dependencies:** CP2-T049
**Blocks:** CP2-T055
**Expected test level:** integration, static
**Graphify required:** yes
**Paper notes required:** yes

## 1. Activation gate
Only proceed if CP2-T049 = **CONTINUE**. Otherwise leave NOT STARTED and record
"gated".

## 2. Purpose
Extend beyond SINGLE_CLIENT to multi-client compromise (pairs and a fixed set of 20
triples) to study compound and co-cluster effects under B4.

## 3. Pre-start audit
Inspect the B4 cluster structure and single-client blast-radius results.

## 4. Scope
- Compromise patterns: all eligible **pairs** (or a fixed sampled set) and **20
  triples**, selected with `compromise_pattern_seed = 400`.
- Each co-victim gets an **independent** poisoning stream by `client_id` in the
  SeedSequence (no shared/added seeds).
- Measure compound Δτ, blast radius, and B4 co-cluster spillover.

## 5. Non-goals
- No change to per-victim injection mechanics; no new policies/datasets.

## 6. Scientific locks
`compromise_pattern_seed=400`; independent co-victim streams via SeedSequence
`client_id`; no integer seed addition; victim-local reservoirs; spillover is
mechanistic.

## 7. Coding locks
Deterministic pattern selection; typed; no in-place mutation; independent streams.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_multiclient
pyright
```
Assert co-victim streams are independent and reproducible.

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
Multi-client compound effects strengthen the policy-differentiation story (esp. B4).

## 11. Acceptance criteria
- Pairs + 20 triples run with independent streams; compound/spillover measured;
  pyright green. (Or "gated".)

## 12. Blocking conditions
Combinatorial blow-up → cap pattern count, record decision.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Multi-client results or "gated" note, paper-notes entry, progress entry.
