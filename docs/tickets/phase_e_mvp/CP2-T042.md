# CP2-T042 — N-BaIoT Artifact Loading Dry-Run

**Phase:** E — MVP
**Type:** implementation
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T039, CP2-T015
**Blocks:** CP2-T043
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** no

## 1. Purpose
Validate that real N-BaIoT clean score artifacts load through the provenance gate and
into the CP2 pipeline read-only, without running any poisoning yet.

## 2. Pre-start audit
Inspect:
```text
src/datp/data/* (catalog, paths, manifests)
CP2-T007/T015 artifact-availability findings
docs/DATP_CP_Roadmap.md §7 (artifacts), §14 (datasets)
```

## 3. Scope
- Load real per-client benign calibration + test scores for `REGIME_A_NBAIOT` via the
  provenance gate (E=1, control-repo origin, DATP split).
- Confirm `reconstruction_error` column, eligibility counts, Calibration-Pending set.
- **Read-only**: no writes, no poisoning, no retraining.

## 4. Non-goals
- No poisoning runs (CP2-T043+).
- No artifact regeneration (FB1 only, gated).

## 5. Scientific locks
E=1; control-repo artifacts; shared scores; read-only; `REGIME_A_NBAIOT` primary.

## 6. Coding locks
Read-only paths; typed loaders; explicit errors on missing artifacts; no fabrication.

## 7. Implementation guidance
Use the CP2-T021 provenance gate + inherited data loaders; emit a load report under
`_ai_tracking/diagnostics/`.

## 8. Tests and diagnostics
```text
pytest tests/integration/data -q
pyright
```
If real artifacts are absent, this is a hard blocker → FB1 / decision record.

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Real N-BaIoT clean scores load through the gate read-only; eligibility/pending
  counts reported; tests + pyright green.

## 12. Blocking conditions
Missing/invalid (E=5) artifacts → hard-stop; record in human_interventions and route
to FB1.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Load report, integration test output, progress entry.
