# CP2-T045 — [REFACTOR] MVP-Prep Cleanup Checkpoint

**Phase:** E — MVP
**Type:** refactor
**Priority:** medium
**Status:** done
**Audited:** 2026-06-16 — Consolidated the single MVP run path: added
`mvp_matrix.py` (locked 1620-cell `Cp2Mvp CellSpec` enumerator, one call over
all 5 training seeds), `mvp_manifest.py` (typed `Cp2MvpManifest`/
`Cp2MvpResultRow` Pydantic schema, no `Cp2CellId` change), `mvp_run.py`
(`run_nbaiot_mvp`/`write_nbaiot_mvp_manifest`, the only path permitted to
produce `nbaiot_mvp_manifest.json`), `objective_for_source()` in
`source_strategies.py` (directional ASR metadata, not a sweep axis), and a
thin `poison.py run-mvp` CLI command gated on `Cp2Stage.NBAIOT_MVP.allow_run`.
Caught and fixed a real bug pre-merge: `run_nbaiot_mvp` originally called
`enumerate_mvp_matrix` once per training seed with a singleton
victims-by-seed dict, but the enumerator always iterates the full locked
5-seed pool internally — this KeyError'd on the second seed of every call.
Fixed by loading all 5 seed collections first, then calling
`enumerate_mvp_matrix` exactly once with the full mapping (caught by the
integration test, not by unit tests, since the unit tests already exercised
the enumerator correctly with a full mapping). Also found the small-scale
integration fixture needed >=4 clients, since the locked B4 `K=3` requires
`eligible_count > k`. 17 unit tests (matrix+manifest+runner) and 2 integration
tests (real small-scale FL run -> full manifest write, both seeds/cells)
pass; 299/299 unit suite green; `pyright` 0 errors; `ruff check` clean;
`graphify update .` ran (7650 nodes, 19194 edges, 437 communities).
**Dependencies:** CP2-T042, CP2-T043, CP2-T044
**Blocks:** —
**Expected test level:** unit, integration, static, graphify
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Refactor checkpoint before launching MVP runs: consolidate data-loading, diagnostic,
and run-plan code; ensure the run path is clean and reproducible.

## 2. Pre-start audit
Re-read CP2-T042–T044 modules and diagnostic scripts; grep for duplicated loaders/
path builders.

## 3. Scope
- Centralize data-loading + path/manifest construction; remove duplicated diagnostic
  glue; ensure single CLI run path.
- Run Graphify to validate the end-to-end run topology before the real MVP.

## 4. Non-goals
- No new features; no scope changes.

## 5. Scientific locks
Read-only artifacts; default policies; SINGLE_CLIENT; locks preserved.

## 6. Coding locks
No duplication; typed; no legacy; single run path.

## 7. Implementation guidance
Consolidate run/diagnostic glue into one owner; keep heavy stages flag-gated.

## 8. Tests and diagnostics
```text
pytest tests/unit tests/integration -q
pyright
make lint
```

## 9. Graphify
Run `graphify update .` (no API key needed); save under `_ai_tracking/graphify/`. Else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Single clean run path; no duplication; tests + pyright + lint green; Graphify
  run/deferred.

## 12. Blocking conditions
If consolidation reveals a run-path gap, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Refactor diff, test/pyright/lint output, Graphify status, progress entry.
