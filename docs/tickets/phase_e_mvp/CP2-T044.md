# CP2-T044 — All-Seeds/One-Victim Diagnostic & MVP Run Plan

**Phase:** E — MVP
**Type:** implementation
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — multi-seed stability confirmed (5/5 sign consistency,
all 6 directional policy×source combos); `mu_flag_threshold` locked per
training seed (5 distinct values); bounded MVP matrix locked (1620 cells);
`Cp2Stage.NBAIOT_MVP.allow_run` flipped True. See
`_ai_tracking/diagnostics/CP2-T044_allseed_one_victim_stability.md`,
`_ai_tracking/run_logs/CP2-T044_mvp_run_plan.md`, decision log entry.
**Dependencies:** CP2-T043, CP2-T018, CP2-T019
**Blocks:** CP2-T046
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Validate multi-seed stability on one victim and finalize the bounded MVP run plan
(cells, seeds, victims, resource budget) before launching.

## 2. Pre-start audit
Inspect CP2-T018 resource budget; CP2-T019 manifest; CP2-T043 diagnostics.

## 3. Scope
- **All seeds / one victim**: run the 5 poisoning seeds [100–104] on one victim;
  confirm paired-delta stability and sign consistency.
- Finalize the MVP matrix: default policies {B1,B2,B4} × objectives {RAISE,LOWER} ×
  sources {RANDOM,HIGH,LOW} × fraction grid × eligible feasible victims × 5 seeds,
  scoped to `REGIME_A_NBAIOT`.
- Write the MVP run plan + resource estimate under `_ai_tracking/run_logs/`.

## 4. Non-goals
- No Full-scope additions (Phase F, gated).
- No execution of the full matrix yet.

## 5. Scientific locks
Seeds [100–104]; two-layer stats; SINGLE_CLIENT; default policies; fraction grid;
`mu_flag_threshold` locked before runs.

## 6. Coding locks
Deterministic; typed plan; resource budget enforced; manifest-backed.

## 7. Implementation guidance
Produce a concrete, bounded run plan; lock `mu_flag_threshold` from clean data now.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_diag
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Document the locked MVP matrix and that `mu_flag_threshold` was fixed pre-poison.

## 11. Acceptance criteria
- Multi-seed stability confirmed; bounded MVP plan + resource estimate written;
  `mu_flag_threshold` locked; pyright green.

## 12. Blocking conditions
If resource budget is exceeded by the plan, shrink scope and record the decision.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Stability report, MVP run plan, paper-notes entry, progress entry.
