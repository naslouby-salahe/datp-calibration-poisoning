# CP2-T046 — MVP Manifest & Result-Sanity Audit

**Phase:** E — MVP
**Type:** results-audit
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — Audited the real `nbaiot_mvp_manifest.json` (1620
cells, 5 seeds, 9 victims, 3 policies, 3 sources, 4 fractions). Verdict: PASS.
Completeness: `n_cells == len(results) == 1620`, re-validated against the
typed `Cp2MvpManifest` schema, 1620 unique cell keys, seed pairing exact,
`target_scope == single_client` on all rows, no `.tmp` artifacts. Provenance:
`local_epochs == 1`; `mu_flag_threshold_by_training_seed` identical to the
CP2-T044 stability-sweep values. Eligibility: `n_eligible == 9`,
`coverage_ratio == 1.0` on every row (no Calibration-Pending clients).
AUROC invariance: True on 1620/1620 rows. `f=0.0` no-op invariant: exact zero
on all 405 zero-fraction rows. Numerical sanity: 0 NaN/Inf across all float
fields. Directional sanity: HIGH raises in 403/405, LOW lowers in 403/405,
RANDOM near-null (mean ~0, balanced pos/neg). Flagged 4 sign-reversed cells,
all `B4_CLUSTER`, mechanistically explained by `Δτ_total = Δτ_agg +
Δτ_churn` cluster-reassignment churn — not a defect. Blast radius by policy
(descriptive): B1=0.559, B4=0.447, B2=0.091, replicating the CP2-T043
qualitative ordering at full scale (resolves the CP2-T043 caveat that B4's
single-seed magnitude (88.9%) was unrepresentative). No significance/claim
interpretation performed (reserved for CP2-T047/T049). Full report:
`docs/tickets/_ai_tracking/audits/CP2-T046_mvp_manifest_result_sanity_audit.md`.
Paper notes appended. Graphify deferred (read-only audit, no code changes —
no topology to validate; see decision log).
**Dependencies:** CP2-T044
**Blocks:** CP2-T047, CP2-T048
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
After the bounded MVP runs execute, audit manifests and result artifacts for
completeness and sanity before any kill-trigger or continue/stop decision.

## 2. Pre-start audit
Inspect the MVP output artifacts and manifests produced under the run-log/results
layout; roadmap §10, §11.

## 3. Scope
- Verify every MVP cell has a complete result (non-empty metrics; no `.tmp`
  placeholders), a manifest with all child seeds, and provenance E=1.
- Sanity checks: cardinality preserved, AUROC invariant, CV(FPR)+coverage present,
  directional effects, feasibility coverage.
- Flag anomalies; do not interpret significance yet.

## 4. Non-goals
- No kill-trigger evaluation (CP2-T047).
- No claim-making.

## 5. Scientific locks
Result exists only if metrics.json non-empty; manifest complete; AUROC invariant;
CV(FPR)+coverage; `mu_flag_threshold` was locked pre-poison.

## 6. Coding locks
Read-only audit; typed; explicit anomaly flags; no fabrication.

## 7. Implementation guidance
Use the results-audit + artifact-audit patterns; write an audit report under
`_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_results
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Record which cells are complete/feasible for later tables.

## 11. Acceptance criteria
- All MVP cells complete and sane, or anomalies explicitly flagged; audit report
  written; pyright green.

## 12. Blocking conditions
Incomplete/corrupt artifacts → route to owning ticket; do not proceed to decision.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Results-audit report, output, paper-notes entry, progress entry.
