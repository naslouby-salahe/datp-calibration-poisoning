# CP2-T047 — Kill-Trigger Evaluation

**Phase:** E — MVP
**Type:** results-audit / decision-input
**Priority:** critical
**Status:** done
**Audited:** 2026-06-16 — Evaluated all 7 pre-registered kill triggers
(roadmap §16) against the audited 1620-cell manifest (CP2-T046) plus a fresh
read-only two-layer statistical pass (bootstrap CI on 5 seed-level
aggregates, sign test, per-victim ≥4/5 sign consistency, strict-majority-of-9
rule) via the existing `datp.attacks.inference` module — no new production
code. **Verdict: no kill trigger fires.** (1) refuted strongly: B2 shows
material, monotone, fully sign-consistent Δτ at every fraction (f=0.40:
+0.914 raise / −0.167 lower, CI excludes zero, 9/9 victims, materiality
1.000). (3) refuted: B1/B2/B4 clearly distinguishable and the separation is
mechanistically explained (B1 ≈ B2/9 via the global-mean dilution; B4
intermediate on both magnitude and blast-radius axes). (4) not applicable —
no diagnostic-upper-bound run exists in the MVP to compare against (CP2's
gray-box-only guard confirmed). (5)/(6)/(7) not fired, re-confirmed by
citing prior cleanliness/contamination audits. **(2) flagged as an open
evidentiary gap, not resolved either way**: the manifest schema carries the
Δτ-materiality (ASR) proxy but not literal victim ΔTPR/ΔBA required by
roadmap §8/§12's downstream-metric leg — computing it is out of scope for
this read-only ticket; recommended as a bounded follow-on reusing existing
`cell_runner.py` primitives, for CP2-T049 to weigh explicitly. Also flagged
(non-trigger-altering): `B4_CLUSTER`/`RANDOM_BENIGN`/`f=0.40` is the one
RANDOM cell (of 9) where the bootstrap CI excludes zero, mechanistically
attributed to cluster-churn variance under the locked
`Δτ_total=Δτ_agg+Δτ_churn` decomposition, magnitude 6–13× smaller than the
directional effects at the same fraction. Full report:
`docs/tickets/_ai_tracking/audits/CP2-T047_kill_trigger_evaluation.md`.
Paper notes appended. Graphify deferred (read-only, no code changes).
**Dependencies:** CP2-T046
**Blocks:** CP2-T049
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Evaluate the pre-registered kill-triggers against the audited MVP results to decide
whether the CP2 hypothesis is viable, weak, or refuted.

## 2. Pre-start audit
Inspect CP2-T046 audit; `docs/DATP_CP_Roadmap.md` §12 (kill-triggers / go-no-go).

## 3. Scope
Evaluate each pre-registered kill-trigger, e.g.:
- HIGH/LOW produce no significant directional Δτ on eligible feasible victims.
- B1 vs B2 vs B4 show **no** policy-differentiated blast radius.
- RANDOM control is **not** near-null (confound).
- AUROC moves materially (would break the calibration-only framing).
- feasibility coverage too low for meaningful claims.
Record the outcome per trigger with evidence; do not fabricate significance.

## 4. Non-goals
- No final continue/stop decision (CP2-T049).
- No Full-scope work.

## 5. Scientific locks
Two-layer stats; bootstrap CI on 5 aggregates; sign test supporting; coverage
reported; honest null reporting.

## 6. Coding locks
Read-only; typed; evidence-backed; no overclaim.

## 7. Implementation guidance
Produce a kill-trigger evaluation report under `_ai_tracking/audits/` with a clear
per-trigger verdict.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_results
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
A clean null is a publishable, honest outcome; record framing either way.

## 11. Acceptance criteria
- Every kill-trigger evaluated with evidence; viability verdict recorded; pyright green.

## 12. Blocking conditions
If a trigger fires ambiguously, gather more diagnostic evidence before CP2-T049.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Kill-trigger report, output, paper-notes entry, progress entry.
