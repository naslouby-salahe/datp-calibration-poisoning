# CP2-T048 — [DRIFT] MVP Drift Check

**Phase:** E — MVP
**Type:** scientific-drift
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — Drift-checked the real-data MVP execution path
against roadmap §5–§12. **PASS, no drift.** Calibration-channel-only
boundary: `auroc_invariant==True` 1620/1620; code path imports only
score/threshold/metric modules, never `federated.protocols.fedavg` or model
modules. E=1/SINGLE_CLIENT/policies/B3-absence: all re-verified directly
against the live manifest (`local_epochs==1`, `target_scope` single_client
on all rows, `policies==[b1,b2,b4]` exactly, 0 B3 hits outside prohibition
context). REPLACE_FIXED_BUDGET/victim-local/no-in-place-mutation: manifest
fields confirm `injection_rule`/`reservoir_mode`; mutation/reservoir
guarantees re-confirmed via the unit suite (unchanged from CP2-T027/T043).
Two-layer stats/CV(FPR)/coverage/`mu_flag_threshold` pre-poison lock:
re-inspected `mvp_run.py` control flow — `mu_flag` is locked before
`enumerate_mvp_matrix`/injection runs, an architectural guarantee not just a
value match. `REGIME_A_NBAIOT` only: `mvp_run.py` hardcodes `Regime.A`, no
code path can reach CICIoT2023/Edge-IIoTset. Seed-scheme integrity:
`entropy[1]==poisoning_seed` on all 1620/1620 rows (full-scale check, not
spot-check). Regression: 484 attacks/thresholding/statistics unit tests +
19 cp2_smoke integration tests pass; pyright/ruff clean on
`src/datp/attacks`. Noted (out of scope): 4 pre-existing DATP-substrate
`tests/e2e/*` collection errors (`SplitFilename` import), confirmed via
`git log` unrelated to any CP2 work — not actioned. Full report:
`docs/tickets/_ai_tracking/audits/CP2-T048_mvp_drift_check.md`. Paper notes
appended. Graphify deferred (read-only audit, no code changes).
**Dependencies:** CP2-T046
**Blocks:** CP2-T049
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Scientific-drift gate over the MVP results path: confirm real-data runs preserved all
protocol locks before the continue/stop/pivot decision.

## 2. Pre-start audit
Compare MVP execution + artifacts against `docs/DATP_CP_Roadmap.md` §5–§12.

## 3. Scope
Verify on real-data runs:
- only calibration channel poisoned (training/model/aggregation/test untouched).
- E=1; shared scores; SINGLE_CLIENT; default policies {B1,B2,B4}; B3 absent.
- REPLACE_FIXED_BUDGET; victim-local; no in-place mutation; AUROC invariant.
- two-layer stats; CV(FPR)+coverage; `mu_flag_threshold` locked pre-poison.
- `REGIME_A_NBAIOT` only (no stretch/forbidden datasets).

## 4. Non-goals
- No new features; no decision.

## 5. Scientific locks
All CP2 locks (README §9).

## 6. Coding locks
Read-only audit; typed; evidence-backed.

## 7. Implementation guidance
Write a drift report under `_ai_tracking/audits/`.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_results
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Confirms the MVP evidence is protocol-clean for the paper.

## 11. Acceptance criteria
- Drift report confirms MVP runs preserved all locks, or flags violations to fix
  before CP2-T049.

## 12. Blocking conditions
Any lock violation in executed runs → block CP2-T049; route to owning ticket.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Drift report, output, paper-notes entry, progress entry.
