# CP2-T056 — Run Final CP2 Experiments

**Phase:** G — Experiment & paper
**Type:** experiment-runner
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T049 (and, if CONTINUE, CP2-T055)
**Blocks:** CP2-T057
**Expected test level:** integration, static
**Graphify required:** conditional
**Paper notes required:** yes

## 1. Purpose
Execute the final, locked CP2 experiment matrix (MVP-only or Full, per the CP2-T049
decision) and produce complete, provenance-stamped result artifacts for the paper.

## 2. Pre-start audit
Read the CP2-T049 decision and (if CONTINUE) CP2-T055 drift report; confirm the run
plan + resource budget; verify all upstream drift gates passed.

## 3. Scope
- Run the locked matrix end-to-end on `REGIME_A_NBAIOT` (plus any CONTINUE-gated Full
  additions), writing complete `metrics.json`, manifests with all child seeds, and
  run logs.
- Re-confirm provenance (E=1), AUROC invariance, CV(FPR)+coverage on the final runs.
- Do not launch heavy runs beyond the authorized plan.

## 4. Non-goals
- No analysis/figures (CP2-T057).
- No new methods or scope creep.

## 5. Scientific locks
Locked matrix from the decision; all CP2 locks (README §9); `mu_flag_threshold` fixed
pre-poison; complete artifacts only (no `.tmp` as success).

## 6. Coding locks
Deterministic; typed; read shared scores; no recompute downstream; resource-bounded.

## 7. Implementation guidance
Drive via the CLI Full/MVP stage; monitor long runs; record run logs under
`_ai_tracking/run_logs/`.

## 8. Tests and diagnostics
```text
pytest tests/integration -q -k cp2_results
pyright
```

## 9. Graphify
Optional; record deferral.

## 10. Paper-writing notes
Record final cell coverage, feasibility, and seeds for the methods/results sections.

## 11. Acceptance criteria
- Final matrix executed; all cells complete with manifests; invariants re-confirmed;
  pyright green.

## 12. Blocking conditions
Heavy-run authorization missing, or incomplete artifacts → stop with handoff; do not
fabricate results.

## 13. Tracking update
Append to progress log + run logs.

## 14. Completion evidence
Run logs, completed artifacts, re-confirmation output, paper-notes entry, progress.
