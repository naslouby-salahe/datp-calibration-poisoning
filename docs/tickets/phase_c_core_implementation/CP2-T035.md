# CP2-T035 — Paired Comparison, Bootstrap/Sign-Test, Manifest & Run Logging

**Phase:** C — Core implementation
**Type:** implementation
**Priority:** critical
**Status:** not started
**Dependencies:** CP2-T019, CP2-T033, CP2-T034
**Blocks:** CP2-T037, CP2-T038
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** yes

## 1. Purpose
Implement the CP2 two-layer statistical inference, manifest emission, and run
logging that ties metrics/diagnostics into reproducible, provenance-stamped results.

## 2. Pre-start audit
Inspect:
```text
src/datp/statistics/{bootstrap,inference}.py
docs/DATP_CP_Roadmap.md §9 (two-layer stats), §10 (manifest, logging)
```

## 3. Scope
- **Two-layer design** (never 9×5=45 independent): (1) per-victim paired seed deltas
  across the 5 poisoning seeds; (2) seed-level aggregates.
- Bootstrap CI on the **5 seed-level aggregates** (percentile default; BCa available).
- Sign test as **supporting** evidence (≥4/5 sign consistency; strict majority of
  eligible feasible victims). Holm correction **descriptive only**.
- Emit the CP2 manifest (CP2-T019) with all child seeds, locks, reservoir mode,
  `mu_flag_threshold`; write run logs.

## 4. Non-goals
- No treating cells as independent; no inferential overreach with Holm.

## 5. Scientific locks
Two-layer inference; paired per-victim deltas; bootstrap on 5 seed-level aggregates;
sign test supporting only; Holm descriptive; manifest complete; reuse inherited
bootstrap/CV.

## 6. Coding locks
Reuse `statistics`; typed result + manifest objects; deterministic; no `Any`; no
duplication.

## 7. Implementation guidance
Build the inference layer on CP2-T033/T034 objects; serialize manifest + run log via
the CP2-T019 schema.

## 8. Tests and diagnostics
```text
pytest tests/unit/statistics -q
pyright
```
Assert: paired structure (not 45 independent); CI computed on 5 aggregates; manifest
round-trips with all seeds.

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
State the two-layer design explicitly; bootstrap CI is primary, sign test supporting,
Holm descriptive.

## 11. Acceptance criteria
- Two-layer inference + manifest + run logging; tests + pyright green; manifest
  complete.

## 12. Blocking conditions
If <2 feasible victims for a cell, record reduced-power limitation.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Inference/manifest diff, tests + pyright, paper-notes entry, progress entry.
