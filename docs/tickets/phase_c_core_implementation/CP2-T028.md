# CP2-T028 — [REFACTOR] Injector/Reservoir Cleanup Checkpoint

**Phase:** C — Core implementation
**Type:** refactor
**Priority:** medium
**Status:** done
**Audited:** 2026-06-16 — refactor checkpoint; legacy prototype modules deleted, 0 importers.
**Dependencies:** CP2-T025, CP2-T026, CP2-T027
**Blocks:** —
**Expected test level:** unit, static, graphify
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Periodic refactor checkpoint after the injector/reservoir/containers are built:
consolidate, remove the legacy prototype remnants, and verify clean ownership.

## 2. Pre-start audit
Re-read CP2-T024–T027 modules; grep for any remaining `shift_magnitude` /
`attack_rate` / `poison_calibration_errors` references.

## 3. Scope
- Remove dead legacy attack code and old-path tests; no wrappers/redirects/aliases.
- Centralize duplicated reservoir/injector helpers; ensure typed boundaries.
- Run Graphify to confirm the attack subsystem structure.

## 4. Non-goals
- No new features.

## 5. Scientific locks
No in-place mutation; victim-local reservoirs; REPLACE_FIXED_BUDGET; SeedSequence.

## 6. Coding locks
No backward compatibility; delete legacy; centralize; typed; no duplication.

## 7. Implementation guidance
Delete retired modules/tests once references are gone; keep a single injector +
reservoir owner.

## 8. Tests and diagnostics
```text
pytest tests/unit/attacks -q
pyright
make lint
rg -n "shift_magnitude|attack_rate|poison_calibration_errors" src tests
```

## 9. Graphify
Run `graphify update .` (no API key needed); save under `_ai_tracking/graphify/`. Else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- No legacy remnants; single ownership; tests + pyright + lint green; Graphify
  run/deferred.

## 12. Blocking conditions
If deleting legacy breaks shared code, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Cleanup diff, grep confirmation, test/pyright/lint output, Graphify status, progress.
