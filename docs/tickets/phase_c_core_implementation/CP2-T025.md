# CP2-T025 — Score Containers & Eligibility Model

**Phase:** C — Core implementation
**Type:** implementation
**Priority:** high
**Status:** not started
**Dependencies:** CP2-T017, CP2-T024
**Blocks:** CP2-T027
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** no

## 1. Purpose
Define typed score containers and the eligibility model that CP2 uses uniformly,
reusing inherited thresholding eligibility where possible.

## 2. Pre-start audit
Inspect:
```text
src/datp/thresholding/eligibility.py   # identify_eligible, tau_global, build_result
src/datp/core/enums.py                 # ClientStatus
docs/DATP_CP_Roadmap.md §7 (eligibility)
```

## 3. Scope
- Typed per-client score container (cal/test_benign/test_attack) and a victim set
  model (eligible-only).
- Eligibility: `n_min=100`; Calibration-Pending receive `tau_global`, excluded from
  CV(FPR) and the victim set, excluded from B4 clustering.
- Reuse inherited eligibility logic; do not duplicate.

## 4. Non-goals
- No poisoning/injection (next tickets).
- No metric computation yet.

## 5. Scientific locks
`n_min=100`; Calibration-Pending handling exactly as DATP; eligible set fixed under
attack (replacement preserves `n_i`); not valid victims when pending.

## 6. Coding locks
Typed dataclasses; reuse, do not duplicate eligibility; no in-place mutation; no
hardcoded `n_min`.

## 7. Implementation guidance
Add CP2 score/victim containers under `attacks/` or `scoring/`, delegating
eligibility to the thresholding module.

## 8. Tests and diagnostics
```text
pytest tests/unit/thresholding/test_eligibility.py -q
pytest tests/unit/attacks -q
pyright
```

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
None.

## 11. Acceptance criteria
- Typed containers + eligibility model; pending clients excluded from victims/CV(FPR);
  tests + pyright green.

## 12. Blocking conditions
If inherited eligibility cannot be reused cleanly, write a decision record.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Container module diff, tests + pyright, progress entry.
