# CP2-T029 — Source Strategies (RANDOM / HIGH / LOW Benign)

**Phase:** C — Core implementation
**Type:** implementation
**Priority:** high
**Status:** done
**Audited:** 2026-06-16 — `src/datp/attacks/source_strategies.py` + tests; RANDOM/HIGH/LOW, diagnostic gated.
**Dependencies:** CP2-T027
**Blocks:** CP2-T038
**Expected test level:** unit, static
**Graphify required:** yes
**Paper notes required:** yes

## 1. Purpose
Wire the three MVP source strategies into the injector and define the diagnostic-only
source, with correct objective→source coupling.

## 2. Pre-start audit
Inspect CP2-T026 reservoirs, CP2-T016 `PoisoningSourceStrategy`, and roadmap §6.

## 3. Scope
- `RANDOM_BENIGN` (negative control): full victim pool.
- `HIGH_SCORE_BENIGN` (raise): upper 10% tail → threshold rises.
- `LOW_SCORE_BENIGN` (lower): lower 10% tail → threshold lowers.
- `LOW_SCORE_TARGETED_REMOVAL_DIAGNOSTIC_ONLY`: diagnostic-only, never in main matrix
  or gray-box claims; gated behind an explicit flag.
- Encode the near-null criterion for RANDOM (audit flag, not auto-kill).

## 4. Non-goals
- Diagnostic source must not enter the MVP/Full matrix or gray-box claims.

## 5. Scientific locks
Sources `{RANDOM,HIGH,LOW}_*BENIGN` for MVP; victim-local tails; with-replacement;
RANDOM near-null criterion is an audit flag.

## 6. Coding locks
Typed dispatch on `PoisoningSourceStrategy`; no hardcoded tail mass; diagnostic
source isolated.

## 7. Implementation guidance
Strategy dispatch returning the reservoir for the injector; keep diagnostic source
clearly separated.

## 8. Tests and diagnostics
```text
pytest tests/unit/attacks -q
pyright
```
Assert: HIGH raises and LOW lowers the per-client threshold on synthetic data;
RANDOM near-null.

## 9. Graphify
Run if available; else defer.

## 10. Paper-writing notes
RANDOM is the negative control; record its role and near-null criterion.

## 11. Acceptance criteria
- Three MVP sources + isolated diagnostic source; directional effects verified on
  synthetic data; pyright green.

## 12. Blocking conditions
If tail definition is ambiguous for some clients, link CP2-FB2.

## 13. Tracking update
Append to progress log.

## 14. Completion evidence
Strategy diff, tests + pyright, paper-notes entry, progress entry.
