\section{Background and Related Work}
\label{sec:background}

\textbf{Federated anomaly detection for IoT.}
Network-level anomaly detection using federated learning allows devices to collaborate on a shared model while keeping raw traffic local~\cite{rey2022federated}.
Autoencoder-based reconstruction-error detectors are a standard approach: each client trains a local encoder-decoder on benign traffic; anomalies are flagged when reconstruction error exceeds a per-client detection threshold~\cite{zhou2017anomaly}.
The N-BaIoT benchmark~\cite{meidan2018nbaiot} provides labeled benign and Mirai/BASHLITE traffic for nine physical devices spanning five device types, establishing a widely used evaluation platform for FL-IDS research.

\textbf{Defenses.}
Existing defenses target the \emph{training phase}.
Byzantine-robust rules, including Krum~\cite{blanchard2017machine}, Trimmed-Mean, and Median~\cite{yin2018byzantine}, filter or downweight outlier gradients before aggregation.
FLTrust~\cite{cao2020fltrust} anchors aggregation to a trusted server dataset.
Differential privacy approaches add noise to local updates to bound update privacy leakage~\cite{geyer2017differentially}; DP is a privacy mechanism for the training/update channel, not a poisoning defense, and we do not treat it as one.
All of the above operate on gradients or model updates; they complete before the threshold-calibration stage begins.

\begin{sloppypar}
\textbf{Threshold calibration and poisoning.}
By construction, a detection threshold at fixed quantile $q$ is sensitive to the scores admitted to the calibration distribution: tail-high contamination raises the boundary (missed detections); tail-low contamination lowers it (false alarms).
Poisoning attacks on anomaly detectors have been studied in centralized settings~\cite{kloft2010online,cretu2008casting,barreno2010security}, e.g.\ Kloft \& Laskov's~\cite{kloft2010online} online poisoning via a continuous training stream; these assume a trusted aggregator or persistent model-update access, both absent from FL's post-training calibration stage, where calibration data never leaves the client and no server-side sanitization is possible.
Prior work on FL model poisoning~\cite{bagdasaryan2020backdoor} and FL inference attacks~\cite{geiping2020inverting} focuses on the gradient/weight surface.
To the best of our knowledge, no prior work isolates post-training threshold calibration as the attacked channel in federated threshold-based IoT anomaly detection under otherwise-unchanged training, model, aggregation, and test artifacts (Table~\ref{tab:related}).
We do not claim a new class of poisoning in the abstract sense; the contribution is isolating the post-training calibration channel in federated threshold-based anomaly detection and showing policy-dependent victim effects and threshold propagation under a controlled score-level intervention.
The closest neighbors treat the calibration/thresholding pipeline as trusted: Laridi et al.~\cite{laridi2024} propose summary-statistics-based federated thresholding without adversarial contamination, and S\'aez-de-C\'amara et al.~\cite{saezdecamara2023} propose a clustered federated IoT anomaly-detection architecture assuming a benign calibration pipeline; DATP-CP instead treats client-local benign calibration as the attacked channel.
\end{sloppypar}

\begin{table}[tb]
\caption{Positioning relative to adjacent poisoning literature. DATP-CP differs by acting strictly after training, on the autonomously collected benign calibration buffer, leaving model weights, gradients, training data, and test data unchanged.}
\label{tab:related}
\centering
\footnotesize
\renewcommand{\arraystretch}{0.92}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{2.5cm}>{\raggedright\arraybackslash}p{2.3cm}>{\raggedright\arraybackslash}p{2.3cm}>{\raggedright\arraybackslash}p{3.3cm}@{}}
\toprule
Prior work type & Attack surface & What changes & Distinction from DATP-CP \\
\midrule
Training-data poisoning~\cite{biggio2012poisoning,tolpegin2020data} & Training set / labels & Model parameters & DATP-CP leaves training data and model unchanged \\
\addlinespace[3pt]
FL model poisoning~\cite{bhagoji2019analyzing,bagdasaryan2020backdoor} & Client gradients & Model weights & Needs no gradient access; acts after convergence \\
\addlinespace[3pt]
Byzantine aggregation~\cite{blanchard2017machine,yin2018byzantine} & Aggregation inputs & Server aggregate & Does not touch aggregation; harm is post-aggregation \\
\addlinespace[3pt]
Centralized AD poisoning~\cite{kloft2010online,cretu2008casting} & Central train stream & Decision boundary & No trusted sanitizer exists in FL calibration \\
\addlinespace[3pt]
Threshold personalization~\cite{ghosh2020efficient,sattler2020clustered} & Threshold policy & Per-client/cluster $\tau$ & Studies utility, not adversarial contamination \\
\addlinespace[3pt]
\textbf{DATP-CP (ours)} & Calibration scores & Threshold $\tau$ only & Score-level, post-training, single-client \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Threshold policies.}
Federated threshold personalization spans \textsc{Global}, \textsc{Local}, and \textsc{Cluster} designs~\cite{ghosh2020efficient,sattler2020clustered} (defined precisely in \S\ref{sec:method}); their security implications have not been studied under calibration-stage adversaries.
